{
  "name": "maptalk-firestore-rules",
  "version": "1.0.0",
  "private": true,
  "description": "Security rules tests for MapTalk, run against the Firestore emulator.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project maptalk-rules-test \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.6.0"
  }
}
